import { WordHeroPage } from "@/components/ui/scroll-hero-section";

export default function Hero() {
  return (
    <WordHeroPage
      items={["Test.", "Break.", "Verify.", "Automate.", "Engineer.", "Perfect.", "Ship."]}
    />
  );
}
